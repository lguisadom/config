application:
  config:
    description: Profile Testing

server:
  port: 9084

spring:
  application:
    name: service-credit
  cloud:
    config:
      profile: qa

  data:
    mongodb:
      database: 'creditdb'
      host: 'localhost'
      port: 27017

#Eureka Client
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8087/eureka
    #enabled: false